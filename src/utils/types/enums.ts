export enum CATEGORY_TYPE {
  complex,
  text,
  number,
  list,
  extra,
}

export enum Order {
  SUSPENDED,
  COMPELETE,
  PROCESSING,
}

export enum Payment {
  SUSPENDED,
  PAID,
  NOT_PAID,
}

export enum Post {
  NOT_YET_POSTED,
  DELIVERED,
  NOT_DELIVERED,
}
